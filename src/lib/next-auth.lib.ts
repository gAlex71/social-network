import NextAuth from 'next-auth';
// import { AuthFormType } from '@/components/screens/auth/auth.types';
// import Credentials from 'next-auth/providers/credentials';

export default NextAuth ({
	providers: [
		
	],
});
